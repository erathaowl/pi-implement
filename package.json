{
  "name": "pi-implement-markdown",
  "version": "0.1.0",
  "description": "A minimal pi extension for sequentially implementing tasks from Markdown files",
  "type": "module",
  "private": true,
  "keywords": [
    "pi-package"
  ],
  "scripts": {
    "test": "node --test tests/*.test.ts"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "license": "MIT"
}
