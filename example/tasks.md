# Pi Implement Smoke Test

All tasks are intentionally small and deterministic.

## Shared constraints

- Work only inside `.pi-implement-smoke/`.
- Do not modify files outside this directory.
- Keep every file minimal.
- Do not install dependencies.
- Do not perform remote Git operations.
- Complete only the requested task.

## Task 1 — Create test directory

Create the directory `.pi-implement-smoke/`.

## Task 2 — Create README

Create `.pi-implement-smoke/README.md` containing:

`# Pi Implement Smoke Test`

## Task 3 — Add description

Append this line to `README.md`:

`Temporary files used to test sequential task execution.`

## Task 4 — Create text file

Create `.pi-implement-smoke/hello.txt` containing:

`hello`

## Task 5 — Add second line

Append this line to `hello.txt`:

`world`

## Task 6 — Create JSON file

Create `.pi-implement-smoke/data.json` with valid JSON containing:

- `name`: `"smoke-test"`
- `enabled`: `true`

## Task 7 — Add JSON version

Add `"version": 1` to `data.json`.

## Task 8 — Create Python file

Create `.pi-implement-smoke/example.py` containing a function `hello()` that returns `"hello"`.

## Task 9 — Add Python function

Add a function `add(a, b)` to `example.py` that returns `a + b`.

## Task 10 — Add Python constant

Add this constant to `example.py`:

`VERSION = 1`

## Task 11 — Create JavaScript file

Create `.pi-implement-smoke/example.js` containing:

`export const enabled = true;`

## Task 12 — Add JavaScript function

Add an exported function `add(a, b)` to `example.js` that returns `a + b`.

## Task 13 — Create YAML file

Create `.pi-implement-smoke/config.yaml` containing:

`enabled: true`

## Task 14 — Add YAML value

Add this value to `config.yaml`:

`name: smoke-test`

## Task 15 — Create list

Create `.pi-implement-smoke/items.txt` with these three lines:

`one`
`two`
`three`

## Task 16 — Add fourth item

Append this line to `items.txt`:

`four`

## Task 17 — Create Markdown checklist

Create `.pi-implement-smoke/checklist.md` containing three unchecked Markdown checklist items named:

- Alpha
- Beta
- Gamma

## Task 18 — Complete first checklist item

Change the Alpha item in `checklist.md` from unchecked to checked.

## Task 19 — Create status file

Create `.pi-implement-smoke/status.txt` containing:

`tasks nearly complete`

## Task 20 — Mark smoke test complete

Replace the contents of `status.txt` with:

`smoke test complete`
